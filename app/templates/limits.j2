{
  "apiVersion": "v1",
  "kind": "LimitRange",
  "metadata": {
     "name": "{{item.name}}",
     "namespace": "{{namespace.name}}",
     {% include "include/labels.j2" %}
     {% include "include/annotations.j2" %}
  },
  "spec": {
    "limits": {{item.limits|to_json}}
  }
}
