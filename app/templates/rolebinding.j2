{
    "kind": "RoleBinding",
    "apiVersion": "rbac.authorization.k8s.io/v1",
    "metadata": {
        "name": "{{ item.name }}",
        "namespace": "{{ namespace.name }}",
         {% include "include/labels.j2" %}
         {% include "include/annotations.j2" %}
    },
    "roleRef": {
        "apiGroup": "rbac.authorization.k8s.io",
        "kind": "Role",
        "name": "{{ item.roleBinding }}"
    },
    "subjects": [
        {
            "kind": "ServiceAccount",
            "name": "{{ item.name }}",
            "namespace": "{{ namespace.name }}"
        }
    ]
}
